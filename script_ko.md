# Neural Rendering 발표 대본 (한국어)

> **발표 시간:** 약 15~20분 (질문 제외)
> **구성:** 슬라이드별 대본 + 핵심 설명 포인트
> **팁:** `S` 키를 누르면 슬라이드에서 짧은 메모가 표시됩니다.

---

## Slide 1 — Cover

안녕하세요, 저는 김시은샘입니다.
오늘 발표 주제는 **Neural Rendering(뉴럴 렌더링)**, 한국어로 하면 "신경망 기반 렌더링"입니다.

요즘 스마트폰으로 공간을 스캔해서 3D(쓰리디) 장면을 만드는 앱들이 있죠.
혹시 Luma AI(루마 에이아이)나 iPhone(아이폰)의 공간 스캔 기능을 써보신 분 계신가요?
이런 기술들의 핵심에 오늘 다룰 Neural Rendering이 있습니다.

오늘 발표 흐름은 이렇습니다.
기존 3D 재구성 방법이 왜 한계에 부딪히는지 먼저 보고,
그 다음 Neural Rendering의 두 가지 핵심 기법인 NeRF(너프)와 3D Gaussian Splatting(쓰리디 가우시안 스플래팅)을 설명하겠습니다.

---

## Slide 2 — Why Classical Methods Fall Short

3D 장면을 만드는 전통적인 방법은 크게 두 가지입니다.

첫 번째는 **수동 모델링**입니다.
Blender(블렌더)나 Maya(마야) 같은 툴로 아티스트가 직접 만드는 방식인데요,
간단한 오브젝트 하나에도 수십 시간이 걸립니다.
결정적으로, 실제 세계를 그대로 담기는 불가능에 가깝습니다.

두 번째는 **Photogrammetry(포토그래메트리), 즉 사진측량법**입니다.
여러 장의 사진에서 자동으로 3D를 복원하는 방법인데,
투명한 물체, 반사 표면, 가는 구조물에서 완전히 무너집니다.

두 방법의 공통적인 한계는 이겁니다.
"**어떤 새로운 시점에서도 사실적으로 보이는 장면을 만들 수 없다.**"
이게 Neural Rendering이 해결하려는 핵심 문제입니다.

---

## Slide 3 — How Photogrammetry Works

Photogrammetry가 어떻게 작동하는지 잠깐 짚고 넘어가겠습니다.
이게 NeRF의 출발점이기도 하거든요.

원리는 간단합니다.
여러 각도에서 찍은 사진들에서 **같은 점을 여러 장에서 찾아서**,
삼각측량으로 3D 좌표를 계산합니다.
이걸 **Structure from Motion(스트럭처 프롬 모션)**, 줄여서 SfM(에스에프엠)이라고 합니다.

결과물은 수백만 개의 3D 점으로 이루어진 **포인트 클라우드(Point Cloud)**입니다.
건물, 동상, 바위 같은 불투명하고 텍스처가 풍부한 대상에는 잘 됩니다.

문제는 유리, 거울, 털, 잎사귀처럼
**보는 각도에 따라 다르게 보이는 표면**에서 실패한다는 겁니다.

오픈소스 툴로는 COLMAP(콜맵)이 표준입니다.
그리고 나중에 보실 NeRF도 이 COLMAP으로 카메라 위치를 추정하는 것부터 시작합니다.

---

## Slide 4 — What is Neural Rendering?

그럼 Neural Rendering은 다른 걸까요?

핵심 아이디어는 이겁니다.
"**3D 형태를 직접 저장하는 대신, 신경망이 장면 자체를 학습한다.**"

기존 방법이 "형태를 저장"한다면,
Neural Rendering은 "어떤 위치에서 어떤 방향을 보면 무슨 색인가?"라는
**질문에 답하는 함수**를 학습합니다.
이 함수를 학습하고 나면, 실제로 찍지 않은 시점에서도 장면을 볼 수 있게 됩니다.

현재 가장 중요한 두 방법이 있습니다.

**NeRF(너프)** — Neural Radiance Field(뉴럴 레이디언스 필드), 2020년
아주 작은 MLP(엠엘피) 신경망 하나가 장면 전체를 암묵적으로 표현합니다.
키워드는 **Implicit(임플리싯) · MLP · Volume Rendering(볼륨 렌더링)**입니다.

**3D Gaussian Splatting(쓰리디 가우시안 스플래팅)** — 3DGS(쓰리디지에스), 2023년
수백만 개의 3D 타원체, 즉 가우시안이 장면을 명시적으로 표현합니다.
키워드는 **Explicit(익스플리싯) · Rasterization(래스터라이제이션) · Real-time(리얼타임)**입니다.

---

## Slide 5 — How NeRF Works

NeRF가 어떻게 작동하는지 파이프라인을 따라 설명하겠습니다.

**① 입력: 사진 50~(쉰 장에서) 200장 + 카메라 위치**
COLMAP으로 각 사진을 찍은 카메라의 3D 위치를 계산합니다.

**② 렌더링할 픽셀마다 Ray(레이, 광선)를 쏩니다**
카메라에서 픽셀을 통해 3D 공간으로 광선을 하나 쏩니다.
현실에서 빛이 눈에 들어오는 경로를 거꾸로 추적하는 겁니다.

**③ 레이 위 점들을 MLP에 입력합니다**
광선 위에 64~(예순네 개에서) 128개의 3D 좌표를 샘플링하고,
각 점의 위치 x(엑스), y(와이), z(지)와
보는 방향 θ(세타), φ(파이) — 5개 숫자를 신경망에 넣습니다.
신경망은 두 가지를 출력합니다: **RGB(알지비) 색깔**과 **σ(시그마) 밀도**.
σ(시그마)가 높으면 그 위치에 뭔가 있다는 뜻이고, 낮으면 빈 공간입니다.

**④ Volume Rendering(볼륨 렌더링)으로 픽셀 색을 만듭니다**
앞에서부터 각 점의 색과 밀도를 합성합니다.
밀도가 높으면 뒤를 가리고, 낮으면 — 공기처럼 — 투명하게 통과합니다.

**⑤ 실제 사진과 비교해서 Loss(로스)를 계산합니다**
예측 픽셀 색과 실제 사진의 차이를 역전파로 줄입니다.
이걸 수백만 번 반복하면 신경망이 장면을 "암기"합니다.
학습 시간은 GPU(지피유) 1개 기준 1~(한 시간에서) 8시간입니다.

---

## Slide 5b — NeRF Result

이게 실제로 어떻게 나오는지 보여드리겠습니다.

화면에 보이는 건 **레고 불도저** 장면입니다.
물리적인 레고 세트를 약 100장의 사진으로 촬영해서 NeRF를 학습시킨 결과입니다.

**3D 모델 파일은 전혀 없습니다.** 아티스트가 만든 것도 없고요.
신경망이 사진만 보고 형태와 외관을 전부 스스로 파악했습니다.
지금 보시는 시점은 실제로 카메라가 있었던 자리가 아닙니다.

오른쪽 위에 있는 instant-ngp(인스턴트 엔지피) UI(유아이)도 보이는데,
이건 NVIDIA(엔비디아)가 만든 오픈소스 구현입니다.
이 정도 품질이 노트북 GPU에서도 몇 분 안에 나옵니다.

---

## Slide 6 — How 3D Gaussian Splatting Works

3DGS는 NeRF와 접근 방식이 다릅니다.
신경망 대신 수백만 개의 **3D 가우시안(Gaussian)**으로 장면을 표현합니다.

파이프라인을 보면:

**① 포인트 클라우드에서 시작합니다**
COLMAP의 출력, 즉 희소한 3D 점들을 가우시안의 초기값으로 사용합니다.

**② 각 점을 3D 가우시안으로 바꿉니다**
각 가우시안은 네 가지 파라미터를 가집니다:
- **μ(뮤)** — 위치, x(엑스), y(와이), z(지)
- **Σ(대문자 시그마)** — 모양과 회전 (이 타원이 어느 방향으로 얼마나 뻗어있는가)
- **SH(에스에이치)** — 색깔, Spherical Harmonics(스페리컬 하모닉스). 보는 방향에 따라 달라지는 색을 표현합니다.
- **α(알파)** — 투명도

**③ 카메라 기준으로 2D 타원으로 투영합니다 — Splatting(스플래팅)**
각 가우시안을 현재 카메라 시점에서 2D 타원으로 납작하게 투영합니다.

**④ 깊이 순으로 정렬하고 합성합니다**
앞에 있는 것부터 알파 합성(alpha composite)하면 최종 픽셀 색이 나옵니다.

렌더링 시 신경망 연산이 전혀 없기 때문에
**실시간으로 30~(삼십에서) 100fps(백 프레임 퍼 세컨드) 이상**이 가능합니다.
학습도 NeRF보다 훨씬 빠릅니다 — 약 30분이면 됩니다.

---

## Slide 6b — 3DGS Result

이게 3DGS로 재구성한 실제 실외 장면입니다.

공원에 세워진 자전거를 카메라로 촬영해서 만든 장면인데,
잔디 한 가닥, 바퀴살, 나뭇잎 하나하나를 보세요.
이 디테일들이 전부 수백만 개의 작은 타원체, 즉 가우시안으로 표현된 겁니다.

NeRF와 달리 이 장면은 **실시간 렌더링**이 됩니다.
게임 엔진에 바로 연결할 수 있을 수준의 속도입니다.

이미지 출처는 SIGGRAPH(시그래프) 2023에 발표된 원논문입니다.

---

## Slide 7 — Results & Real-world Examples

실제로 이런 기술을 쓰는 툴들을 보겠습니다.

**NeRF Studio(너프 스튜디오)** — UC Berkeley(유씨 버클리)에서 만든 오픈소스 프레임워크입니다.
NeRF의 다양한 변형 모델들을 쉽게 학습하고 시각화할 수 있습니다.

**Luma AI(루마 에이아이)** — 아이폰 앱으로, 물체 주변을 걸어다니며 영상을 찍으면
몇 분 안에 고품질 NeRF를 만들어줍니다. 제품 사진, 부동산 촬영에 실제로 쓰입니다.

**3DGS 원논문 — Kerbl et al.(커블 앤 동료들)** — SIGGRAPH(시그래프) 2023에 발표됐고, 코드가 GitHub(깃헙)에 공개되어 있습니다.

Before(비포) / After(애프터)를 보면 차이가 명확합니다.
기존 Photogrammetry의 결과물은 구멍이 뚫려있고 디테일이 없는 반면,
Neural Rendering은 어느 시점에서도 사실적인 이미지를 만들어냅니다.

---

## Slide 8 — Pros & Cons

장단점을 정리하겠습니다.

**장점:**
- **사실적인 품질** — 기존 방법과는 비교가 안 됩니다.
- **완전 자동** — 사진만 있으면 됩니다. 수동 모델링 필요 없음.
- **새로운 시점 합성** — 실제로 찍지 않은 각도에서 볼 수 있습니다.
- **작은 용량** — NeRF 전체가 MLP 가중치 약 5MB(메가바이트)입니다.

**단점:**
- **학습 시간** — NeRF는 1~(한 시간에서) 8시간, 3DGS는 30분이지만 여전히 오래 걸립니다.
- **실시간 렌더링** — NeRF는 실시간이 안 됩니다. 3DGS는 됩니다.
- **편집이 어렵습니다** — 오브젝트 색깔 바꾸기, 지우기가 매우 어렵습니다.
  일반적인 3D 메시(mesh)처럼 자유롭게 편집이 안 됩니다.
- **정적인 장면 가정** — 사람이나 차가 움직이면 무너집니다.

---

## Slide 9 — Current Limits & What's Next

마지막으로 현재 남아있는 한계와 앞으로의 방향을 보겠습니다.

**현재 한계:**

첫째, **동적 장면** — NeRF와 3DGS 둘 다 장면이 완전히 정적이라고 가정합니다.
사람이 걸어다니거나 차가 지나가면 결과물이 망가집니다.
이를 해결하려는 D-NeRF(디너프), 4D Gaussian(포디 가우시안) 같은 연구들이 활발히 진행 중입니다.

둘째, **대규모 장면** — 도시 규모의 장면을 학습하려면
구역을 나눠서 분산 학습해야 합니다. 아직 쉽지 않습니다.

셋째, **Relighting(리라이팅)** — 학습된 색깔에는 당시 조명이 그대로 박혀있습니다.
조명 방향을 바꾸거나 다른 환경에 놓으려면 별도의 기법이 필요합니다.

**앞으로의 방향:**
- 모바일에서 실시간 NeRF
- 텍스트 한 줄로 3D 장면 생성 — DreamFusion(드림퓨전), Shap-E(쉐입이)
- Unreal Engine(언리얼 엔진), Unity(유니티) 같은 게임 엔진과의 통합
- 로보틱스(Robotics), 의료 영상 분야 응용

---

## 마무리

정리하면:

기존 3D 재구성은 사진측량법까지 왔지만, 사실적인 새 시점 합성은 여전히 어려웠습니다.
NeRF는 신경망으로 이 문제를 처음으로 풀었고,
3DGS는 실시간 렌더링까지 가능하게 만들었습니다.

아직 동적 장면, 편집, 대규모 씬 같은 과제가 남아있지만,
이 분야는 2020년 이후 매년 큰 도약을 하고 있습니다.

질문 있으시면 받겠습니다. 감사합니다.

---

*발표 자료: https://hundredman.github.io/cg-neural-rendering/*
